uploadArchives {
    repositories {
        mavenDeployer {
            pom.artifactId = 'fluency-core'
            pom.project {
                name 'fluency-core'
                description "Yet another fluent logger: core module"
            }
        }
    }
}
